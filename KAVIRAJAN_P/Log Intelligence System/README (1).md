This project implements a Log Intelligence Assistant that allows users to query and analyze application logs using natural language. The system converts user queries into SQL, executes them on a structured log database, and returns relevant results along with meaningful insights.

It is designed to help identify errors, performance issues, and patterns in logs by combining query generation with an insight layer. This enables users to understand system behavior and detect potential issues without manually writing complex queries.
